# Moved: Deterministic Encodings for COSE

The COSE deterministic-encoding declaration draft
(`draft-mih-sokolov-cose-deterministic-encodings`, formerly developed here as
`draft-mih-sokolov-cose-payload-preimage-encoding`) now lives in its own
repository:

**https://github.com/action-state-group/cose-deterministic-encodings**

The commit history and authorship were carried over with `git filter-repo`. This
directory is retained only as a pointer so that links into the earlier
pull-request history (including the review by Amaury Chamayou) keep resolving.
New work happens in the repository above.
